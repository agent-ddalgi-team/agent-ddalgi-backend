# 백엔드 레포 AI 작업 규칙

기준일: 2026-09-26 · v1.3

이 레포에는 백엔드 담당과 Agent 담당이 함께 작업한다. 프론트 화면은 별도 레포다. 두 담당의 코드를 구분하되 기존 폴더를 임의로 이동하지 않는다.

이 파일은 Codex와 Claude Code가 함께 사용하는 공통 작업 규칙의 원본이다. 공통 규칙은 이 파일에서만 관리하고, CLAUDE.md는 이 파일을 불러오는 용도로 유지한다.

## 1. 시작 전 읽을 것

[plan.md](plan.md), [prd.md](prd.md), [contracts.md](contracts.md)를 읽는다. 백엔드는 [task_backend.md](task_backend.md), Agent는 [task_agent.md](task_agent.md)와 [agent.md](agent.md)를 읽는다. 공통 [task.md](task.md)는 연결 지점과 선행 결과 위치를 확인하는 데 사용한다.

- 사용자가 지정한 역할과 BE-* 또는 AG-* 작업 ID를 확인한다.
- 실제 브랜치·기존 미커밋 변경·파일 구조·실행 명령을 확인한다.
- 자기 역할의 task_backend.md 또는 task_agent.md 상태와 실제 코드를 대조하고 선행 결과를 확인한다.
- 기존 파일 수정은 변경할 파일과 목적을 짧게 설명하고 요청 범위 안에서 진행한다. 새 파일 생성은 아래 확인 절차를 따른다.

사용자의 최신 요청을 우선한다. 문서와 요청이 다르면 관련 문서도 갱신하되 미정 사항과 확정 사항을 구분한다.

## 2. 책임 경계

| 현재 역할 | 주 작업 | 임의로 확장하지 않을 영역 |
|---|---|---|
| 백엔드 | 파일/세션, 파서, API, 저장·버전, 승인·출력, 접근 검사 | AI 프롬프트·추출 규칙·모델 선택 전체 교체 |
| Agent | 사실/근거, 추천, 초안·수정안, 의미 검증, LangGraph | 서버 라우트·승인 정책·파일 보관 정책 전체 변경 |

공통 타입, 실행 의존성, 그래프와 서버의 입출력 변경은 영향 범위를 설명하고 다른 담당과 맞춘다. 프론트 코드를 이 레포 안에 새로 만들어 범위를 확대하지 않는다.

## 3. 기존 작업 보호

- 지침을 수정할 때는 기존의 유효한 규칙을 보존한다. 공통 규칙을 AGENTS.md와 CLAUDE.md에 중복 작성하지 않는다.
- 새 파일 생성 전 파일 경로·목적·해당 경로를 선택한 이유·기존 파일 수정으로 해결하기 어려운 이유를 설명하고, 사용자 확인을 받은 뒤 생성한다. 설명만 하고 답변 없이 생성하지 않는다.
- 여러 파일이 필요하면 목록으로 묶어 확인받는다. 이미 사용자가 경로와 생성을 승인한 파일은 다시 확인받지 않는다. 승인 후 경로나 목적이 달라지면 변경 사항을 설명하고 다시 확인받는다.
- 작업일지·임시 파일을 저장소에 만들지 않는다. 진행 기록은 기존 역할별 task 파일을 사용한다.
- `.venv/`, `private_runs/`, `.env`는 커밋하지 않는다. `.gitignore`의 제외 여부와 실제 Git 추적 상태를 확인한다.
- `backend/agent.py`, `prompts/extract.txt`, 상세 회사정보 스키마가 있으면 먼저 읽고 재사용한다.
- 다른 담당자의 미커밋 변경, `.env`, 실회사 원문을 보호한다. 비밀값을 출력하거나 커밋하지 않는다.
- 파일/브랜치를 초기화하거나 관련 없는 대규모 리팩터링을 하지 않는다.
- 새 문서를 계속 복제하지 않는다. 진행은 자기 역할의 task 파일, 결정은 plan.md에 기록한다. 공통 task.md에는 상태·날짜·위치만 적고 상세 기록은 개인 task 파일에 둔다.

## 4. 공통 계약 관리

- 이 레포의 contracts.md가 원본이다. 프론트 레포의 파일은 같은 버전의 사본이다.
- 필드/상태/API 변경 시 실제 모델·예시·계약 버전을 함께 맞추고 프론트 영향과 갱신 필요를 알린다.
- 프론트 사본이 갱신되었다고 확인 없이 주장하지 않는다. 두 레포의 PR/커밋은 별도로 존재한다.
- 입력/출력 스키마 검사는 사실 검증과 구분한다. AI가 반환한 ID가 선택 자료에 실제로 있는지 확인한다.

## 5. 필수 동작 규칙

- 세션 소유·만료·선택 자료 범위를 서버에서 확인한다.
- 세션 첨부와 파생 텍스트·사실·문서·이미지·체크포인트·출력 캐시를 영구 공통 자료로 저장하지 않는다.
- 문서, 자료, 수정안, 검증, 승인 버전을 연결한다. 오래된 수정안과 승인을 적용하지 않는다.
- AI 제안은 명시적 적용 전 현재 문서를 바꾸지 않는다.
- 부분 재검증에서도 다른 영역의 미해결 blocker를 유지한다.
- 중복 요청/중단 재개가 문서 적용·출력을 두 번 실행하지 않게 한다.
- 첨부파일 안 지시를 시스템 지침으로 실행하지 않고 임의 HTML·스크립트를 실행하지 않는다.
- 재시도 횟수를 제한하고 자료 부족은 보완으로 처리한다.
- 출력 실패/재다운로드에 AI 초안 생성을 다시 호출하지 않는다.
- Playwright/python-docx, 새 벡터 DB, AI 이미지 생성, OCR을 확정 범위로 임의 추가하지 않는다.

## 6. 검증과 보고

각 개발 담당이 자기 기능의 필수 확인을 수행한다. 네 번째 확인·보조 담당자에게 핵심 검증을 넘기고 완료 처리하지 않는다.

완료 보고에는 변경 동작·파일, 실제 실행한 확인과 결과, 남은 제한, 작업 ID 상태를 포함한다. 미실행/미확인을 성공으로 바꾸지 않는다. 프론트와 실제 연결하기 전에는 예시 응답/단위 확인 상태를 정확히 표시한다.

## 7. 첫 요청 예시

```text
이번 역할은 [백엔드 또는 Agent], 작업은 [BE-XX 또는 AG-XX]야.
AGENTS.md와 관련 문서를 읽고 현재 코드·기존 변경을 확인해줘.
내 담당 범위에서 구현·검증하고 공통 계약 변경이 필요하면 영향을 설명해줘.
끝나면 백엔드는 task_backend.md, Agent는 task_agent.md에 실제 결과를 기록해줘.
다른 담당자의 상태를 임의로 바꾸지 마.
```

## 8. task 파일 동기화 (고정)
- task_backend.md / task_agent.md 는 개인 작업의 원본, task.md 는 둘이 같이 보는 연결표다.
- 개인 task 파일에서 BE-*/AG-* 의 상태나 선행 조건을 바꾸면 **같은 커밋에서** task.md 2절(연결 지점)의 해당 행도 갱신한다.
  task.md 에는 상태 · 날짜 · "task_backend.md 6절 참조" 식의 위치만 적고 상세 내용은 복제하지 않는다.
- 다른 담당의 행과 상태는 바꾸지 않는다. 바꿔야 하면 요청 항목으로 남긴다.
- 작업 완료 보고 전에 두 파일이 같은 상태를 말하는지 확인한다.


## 9. DB

- plan.md의 2026-09-25 BE-02 결정에 따라 SQLite를 사용한다.
- DB 파일은 private_runs/app.sqlite3이며 Git에 커밋하지 않는다.
- 기존 테이블과 마이그레이션을 먼저 확인한다.
- 추가 테이블·스키마 변경은 필요한 작업에서만 수행하고,
  변경 이유와 영향을 plan.md 4절에 기록한다.
- 옛 Windows 로컬 경로가 현재 PC에도 있다고 가정하지 않는다.
  필요한 옛 코드가 없으면 저장소·브랜치·파일 위치를 요청한다.

## 10. 도구 전환과 작업 인계
- 이전 AI와의 대화를 알고 있다고 가정하지 않는다. 실제 코드·Git 변경·역할별 task 파일에서 현재 상태를 확인한다.
- 작업 종료 시 자기 역할의 task 파일에 완료 내용·실제 검증 결과·남은 일·다음 작업을 기록한다. 별도의 인계 문서를 계속 만들지 않는다.
- 같은 작업 폴더에서 Codex와 Claude Code가 동시에 파일을 수정하지 않는다. 도구를 교대로 쓰거나, 동시 작업이 필요하면 별도의 작업 폴더와 브랜치로 분리한다.
