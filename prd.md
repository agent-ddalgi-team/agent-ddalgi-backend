# 백엔드·Agent 제품 요구사항

기준일: 2026-09-23 · v1.2 · 적용: 백엔드 레포

백엔드 담당은 서버 기능, Agent 담당은 AI 의미 처리와 그래프를 구현한다. 프론트 화면 요구사항은 프론트 레포의 prd.md에서 관리한다. 공통 API 원본은 이 레포의 contracts.md다.

## 1. 해결하려는 문제

회사소개서를 만들 때 자료가 여러 파일에 흩어져 있고, 목적에 맞는 내용을 고르고 분량을 맞추며 사실을 확인하는 작업이 반복된다. 등록 자료와 이번에 추가한 자료를 함께 활용해 근거가 연결된 초안을 만들고, 사용자가 편집한 뒤 승인하여 파일로 받을 수 있게 한다.

사용자는 회사의 문서 작성 담당자다. 최종 내용의 선택·확인·승인을 맡는다. 시스템은 근거 정리, 문구·구성 작성, 누락·불일치 안내, 파일 출력을 지원한다.

## 2. 범위

### 기본 MVP

- 자료 선택, 세션 전용 첨부, 파일별 읽기 상태.
- 목적·강조 내용·작성 방향·목표 페이지 수 입력 및 사전 점검.
- 출처가 연결된 초안과 선택 사진의 자동 배치.
- 목차·페이지·블록 직접 편집, 선택 영역의 AI 문구 생성/수정.
- 사진 후보 찾기, 교체, 추가 업로드, 사진 자리 표시.
- 검증 문제 확인·해결, 버전 저장, 최종 승인.
- 실제 PDF/DOCX 생성·다운로드 및 실패 재시도.

### 기본 MVP에 포함하지 않음

- 자유 좌표·회전·복잡한 도형 편집, 실시간 다중 사용자 공동 편집.
- PPT 출력, 외부 웹에서 임의 사진 수집.
- 설명용 이미지 자동 생성: 확장 지점만 마련하며 별도 선택 기능으로 둔다.
- 자동 OCR: 형식별 기본 읽기를 먼저 구현하고 스캔 자료는 보완 경로를 제공한다.
- 세션 첨부를 영구 회사 자료로 자동 등록, 세션 간 임시 초안 재사용.

## 3. 사용자 단계

| 단계 | 화면 구성 | 사용자의 주요 행동 |
|---|---|---|
| 1. 자료 선택·사전 확인 | 등록 자료, 이번 첨부, 목적·강조·분량, 점검 결과 | 선택/제외, 업로드, 점검 결과 확인 |
| 2. 초안 편집 | 왼쪽 목차·페이지·블록, 오른쪽 페이지, 필요한 때 AI 요청/확인 패널 | 직접 편집, 문구 요청, 사진 선택, 문제 해결 |
| 3. 승인·출력 | 최신 문서 미리보기, 미해결 요약, PDF/DOCX 선택, 최종 승인 | 내용 확인, 승인, 파일 받기 |

## 4. 백엔드 요구사항

| ID | 기능 | 동작 및 인수 기준 |
|---|---|---|
| BR-01 | 세션·접근 범위 | 소유 범위와 만료를 서버에서 검사한다. 파일 ID/URL을 바꿔 다른 세션 자료를 읽지 못한다. |
| BR-02 | 파일 수신·읽기 | 형식/크기/개수 제한을 설정으로 관리한다. 실제 형식과 파서 결과를 확인하고 파일별 상태·구간·원인 코드를 반환한다. |
| BR-03 | 임시 데이터 정리 | 원본, 추출문, 이미지, 임시 검색 데이터, 파생 사실·초안·수정안·체크포인트·출력 캐시를 세션 범위로 관리하고 종료/만료 시 정리한다. |
| BR-04 | 자료 조회·검색 | 사용자가 선택한 등록 자료와 현재 세션 자료만 검색한다. 공통 DB에 세션 업로드가 유입되지 않는다. |
| BR-05 | 사전 확인 버전 | 입력이 바뀌면 input_revision을 증가시키고 점검·확인을 무효화한다. 오래된 점검 결과로 생성하지 않는다. |
| BR-06 | AI 실행 | 자료 분석·초안 작성·검증을 분리한다. 구조화 결과를 검사하고 근거 없는 필드/ID를 거부하거나 확인 필요로 표시한다. |
| BR-07 | 편집안·문서 저장 | AI 제안과 적용된 문서를 분리한다. expected_revision으로 충돌을 방지하고 블록/출처 ID를 유지한다. |
| BR-08 | 재검증·문제 상태 | 변경된 문장과 연관된 근거/문제를 다시 검사한다. 순서만 바꾼 경우 불필요한 사실 질문을 다시 만들지 않는다. |
| BR-09 | 승인 강제 조건 | 최신 버전, 최신 자료 점검, 검증 완료, 필수 문제 해결, 선택 형식의 배치 검사 완료를 서버에서 검사한다. |
| BR-10 | 출력 어댑터 | 승인된 불변 문서 스냅샷으로 PDF/DOCX를 생성한다. 동일 원본 내용·사진을 사용하고 승인 시점의 임시 이미지도 세션 내에서 고정한다. |
| BR-11 | 재시도·중복 방지 | 동일 적용·재개·승인·출력 요청을 중복 실행하지 않는다. 출력 재시도에 AI 초안 생성을 다시 호출하지 않는다. |
| BR-12 | 오류·진행 정보 | 사용자 행동으로 해결 가능한 메시지, 재시도 가능 여부, 작업 ID를 반환한다. 민감한 원문·키를 로그에 노출하지 않는다. |

## 5. 에이전트 책임

| 역할 | 입력 | 출력 |
|---|---|---|
| ① 자료 분석·기획 | 읽기 결과, 선택 자료, 목적·강조·분량 | 사실/근거, 충돌·누락, 구성·분량·사진 추천 |
| ② 구성·초안 작성·편집 보조 | 확인된 사실, 작성 요청, 현재 문서, 선택 영역 | 최초 초안 또는 적용 전 수정안·사진 후보 |
| ③ 초안 검증 | 문서/변경 영역, 사실·원문, 요구사항 | 문제 위치·사유·수정 제안, 검증 결과 |

자세한 흐름과 제한은 agent.md를 따른다. 에이전트 수와 모델 수는 같을 필요가 없다. PDF/DOCX 출력, 파일 읽기, 권한, 일반 재시도는 별도 핵심 AI 역할로 만들지 않는다.

## 6. 사실·검증 기준

- 최소 필수 내용은 회사명과 주요 사업/공정 설명이다. 선택한 작성 목적에 따른 추가 필수 항목은 사전 점검에서 명시한다.
- 인증은 모든 소개서의 필수 항목이 아니다. 포함하면 명칭·적용 범위·기준일/유효기간 등 해당 주장을 뒷받침하는 근거를 확인한다.
- 수치형 납기를 포함하면 적용 품목·수량·조건·예외를 근거에 맞춰 표현한다. 자료에 없는 조건은 만들지 않는다. 숫자형 주장을 제외하는 선택도 지원한다.
- 파일 간 충돌은 원문과 위치를 제시한다. ‘더 최신처럼 보임’만으로 자동 확정하지 않는다.
- 검증은 원자료와 초안의 일치 및 누락 확인이다. 법적 진위나 실제 인증 상태를 보증한다고 표현하지 않는다.
- 사진의 원본 종류를 구분한다. 실제 회사 사진으로 소개할 자료를 AI로 만들어 대체하지 않는다.

## 7. 저장·승인·출력 규칙

- 모든 문서 변경은 문서 버전을 증가시킨다. AI 수정안의 제안만으로는 증가하지 않는다.
- 재검증은 저장/AI 적용 후 수행한다. 타이핑할 때마다 LLM을 호출하지 않는다.
- 해결 기록은 근거·문장과 연결한다. 관련 내용이 바뀌면 재확인하고 무관한 기록은 유지한다.
- UI에서 승인 버튼을 숨기는 것과 별개로 서버에서도 승인/출력 조건을 검사한다.
- 목표 페이지 수와 실제 PDF 쪽수를 구분한다. 차이가 나면 보이고 사용자가 수정하거나 수용하게 한다.
- DOCX는 글과 사진을 후속 편집할 수 있어야 한다. 전체 페이지를 한 장의 이미지로 붙인 결과는 통과시키지 않는다.
- DOCX는 열람 프로그램·폰트에 따라 쪽 나눔이 달라질 수 있다. PDF와 쪽수 일치를 보장하지 않는다.
- 다운로드 완료 파일은 사용자 기기에 남는다. 서버의 세션 종료는 사용자 다운로드 파일을 삭제하지 않는다.

## 8. 주요 예외와 기대 결과

| 상황 | 기대 결과 |
|---|---|
| 암호화/손상/지원하지 않는 파일 | 해당 파일 읽기 불가, 다른 자료 유지, 보완·제외 가능 |
| 스캔 PDF 또는 불확실한 표 | 읽기 불가/일부 확인 필요를 구분하고 불확실한 값은 확정 근거로 사용하지 않음 |
| 사진만 선택 | 텍스트 근거 추가 요청; 회사 사실을 사진으로 추정하지 않음 |
| 사진 없음 | 글 중심 생성; 필요 시 업로드·사진 자리 제공 |
| 자료 간 수치 충돌 | 초안 편집에서 해당 주장 해결/제외, 미해결 필수 주장은 승인 차단 |
| AI 결과 형식 오류 | 제한된 재시도 후 오류 표시; 기존 초안 보존 |
| 오래된 수정안 적용 | 충돌 응답과 새 문서 기준 재요청; 현재 문서를 덮어쓰지 않음 |
| 승인 후 문서 수정 | 승인 해제, 관련 검사 후 재승인 |
| 출력 도중 실패 | 승인본 보존, 같은 버전으로 재시도, AI 재생성 없음 |
| 세션 만료/종료 | 접근 차단·임시 데이터 정리, 새 세션에 내용 유출 없음 |

## 9. 이 레포의 완료 기준

- 백엔드: 세션 격리·파일 읽기·버전·승인·실제 PDF/DOCX·종료/만료 정리를 확인한다.
- Agent: 사실/출처·충돌/누락·문구/구성·사진 후보·변경 후 검증을 확인한다.
- BE 작업은 task_backend.md, AG 작업은 task_agent.md를 따른다. QA-01~QA-20의 결과 위치는 공통 task.md에서 확인하며 지정 개발 담당자가 필수 검증을 수행한다.
- 확인·보조 담당자의 가용 시간을 필수 검증의 선행 조건으로 두지 않는다.
- 코드·서버 연결·실제 파일을 확인한 결과만 완료로 기록한다. 정확도·속도·비용 수치는 실제 평가 뒤 기록한다.

## 10. 두 레포의 API 연결

contracts.md를 실제 API 모델과 맞추고 같은 버전의 사본·예시 응답을 프론트 담당에게 전달한다. Agent와 백엔드 내부 함수 경계도 같은 객체를 사용한다. UI가 보내는 승인 가능 상태를 신뢰하지 않고 서버에서 재검사한다.

프론트와 API가 다른 origin에서 실행되는 경우 허용 origin, 인증/쿠키, 오류 응답과 이미지/파일 응답을 맞춘다. 레포가 둘이라는 이유만으로 별도 도메인 배포를 강제하지 않는다. [FastAPI CORS 공식 문서](https://fastapi.tiangolo.com/tutorial/cors/)를 실제 설정의 참고로 사용한다.

## 11. 기술 참고

- [FastAPI 파일 업로드](https://fastapi.tiangolo.com/tutorial/request-files/): 업로드 요청 구현 참고. 임시 파일 수명과 세션 정책은 본 서비스에서 별도 구현한다.
- [LangGraph Graph API](https://docs.langchain.com/oss/python/langgraph/graph-api): 상태·노드·분기를 구성할 때 참고.
- [LangGraph Interrupts](https://docs.langchain.com/oss/python/langgraph/interrupts): 사용자 확인 후 재개 구현 참고.

공식 문서는 도구 동작의 참고이며 본 PRD의 제품 정책이나 구현 완료를 보증하지 않는다.
